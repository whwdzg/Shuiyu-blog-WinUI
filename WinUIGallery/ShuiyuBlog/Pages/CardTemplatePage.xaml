<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinUIGallery.ShuiyuBlog.Pages.CardTemplatePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:WinUIGallery.ShuiyuBlog.Models"
    mc:Ignorable="d">

    <Grid Padding="20" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="4">
            <TextBlock x:Name="HeaderTextBlock" FontSize="28" FontWeight="SemiBold" Text="详情模板" />
            <TextBlock x:Name="SubHeaderTextBlock" Opacity="0.75" Text="卡片化布局 + 图片预览 + 外链" />
        </StackPanel>

        <ScrollViewer Grid.Row="1">
            <ItemsRepeater x:Name="CardsRepeater">
                <ItemsRepeater.Layout>
                    <StackLayout Spacing="10" />
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="model:DetailCardItem">
                        <Border Padding="12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="10">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="220" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <Border Width="220" Height="120" CornerRadius="8" Background="{ThemeResource ControlFillColorDefaultBrush}">
                                    <Image Source="{x:Bind ImagePath}" Stretch="UniformToFill" />
                                </Border>

                                <StackPanel Grid.Column="1" Spacing="8">
                                    <TextBlock FontSize="20" FontWeight="SemiBold" Text="{x:Bind Title}" TextWrapping="WrapWholeWords" />
                                    <TextBlock Opacity="0.8" Text="{x:Bind RelativePath}" TextWrapping="WrapWholeWords" />
                                    <TextBlock Text="{x:Bind Description}" TextWrapping="WrapWholeWords" />
                                    <Button Content="打开外链" Tag="{x:Bind ExternalUrl}" Click="OpenExternalButton_Click" HorizontalAlignment="Left" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>
