<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinUIGallery.ShuiyuBlog.Pages.BaseConvertToolPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="26" FontWeight="SemiBold" Text="进制转换工具" />

        <TextBox x:Name="InputTextBox" Grid.Row="1" Header="输入数值" PlaceholderText="例如 FF 或 1010" />

        <Grid Grid.Row="2" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ComboBox x:Name="FromBaseComboBox" Header="源进制" />
            <ComboBox x:Name="ToBaseComboBox" Grid.Column="1" Header="目标进制" />
        </Grid>

        <StackPanel Grid.Row="3" Spacing="12">
            <Button Content="转换" Click="ConvertButton_Click" HorizontalAlignment="Left" />
            <TextBlock x:Name="ResultTextBlock" FontSize="18" Text="结果：" TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</Page>
